security:
    providers:
        in_memory:
            users:
                johannes: { password: test }
    
    encoders:
        Symfony\Component\Security\Core\User\UserInterface: plaintext
        
    firewalls:
        default:
            form_login: ~
            anonymous:  ~
            logout:     ~
